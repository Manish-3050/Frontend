export interface contacts {
    id: Number;
    name: string;
    image: string;
    isGroup:boolean;
    participants:Number;
  }

  export interface userDatatype {
    id: Number;
    firstName: string;
    lastName: string;
    avtar: string;
  }